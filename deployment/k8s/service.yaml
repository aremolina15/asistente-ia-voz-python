apiVersion: v1
kind: Service
metadata:
  name: devops-voice-assistant
  namespace: devops
  labels:
    app: devops-voice-assistant
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: devops-voice-assistant
